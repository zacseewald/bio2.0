import React, { Component } from 'react'
import Contact from "../components/Contact";

export class contact extends Component {
  
  render() {
    return (
      <div style={{  background: "linear-gradient(180deg, black -3%, transparent 15%)" }}>
        <Contact />
      </div>
    )
  }
}

export default contact
